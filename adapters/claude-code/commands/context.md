---
description: Load persistent memory (stable profile + recent context) for this project.
---

Call the `context` tool from the **memeora** MCP server (omit `scope` to use the
current project) to fetch the stored stable facts/preferences and recent
episodes for this project. Briefly summarize what's relevant, then continue.
Use the memory naturally — don't force it.
